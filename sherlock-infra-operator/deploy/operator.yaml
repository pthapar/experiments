apiVersion: apps/v1
kind: Deployment
metadata:
  name: sherlock-infra-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: sherlock-infra-operator
  template:
    metadata:
      labels:
        name: sherlock-infra-operator
    spec:
      serviceAccountName: sherlock-infra-operator
      containers:
        - name: sherlock-infra-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - sherlock-infra-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "sherlock-infra-operator"
